This repository contains a MatLab function plotConvergence for plotting various stages of approximations of fractals, alongside several examples.

The function takes as input the variables seed, iter, ifs, plotShape.

(seed, iter, ifs, plotShape)
function [A] = 
